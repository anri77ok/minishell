#include "minishell.h"
#include "tokenization.h"
#include "utils.h"

int	main(void)
{
	//t_token	*token_list;
	char	*cmd_line;

	//token_list = NULL;
	while (1)
	{
		cmd_line = readline("MINISHELL HOP:");
	}
}